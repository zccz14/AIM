# Server Port 8192 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用最小改动把 `modules/api` 的默认监听端口从 `3000` 调整为 `8192`，并同步修正当前 live 文档中的旧端口示例，同时保持 `PORT` 环境变量覆盖行为不变。

**Architecture:** 直接修改 `modules/api/src/server.ts` 中现有默认端口常量，不调整 `PORT` 解析流程、启动入口或回退结构。文档侧仅修正 `docs/api/README.md` 中当前仍会误导读者使用旧端口的本地启动示例与访问地址，不触碰历史计划文档。

**Tech Stack:** TypeScript、Node.js、pnpm、Biome、ripgrep。

---

## 文件结构与职责映射

- Modify: `modules/api/src/server.ts:7-9` - 将默认端口常量从 `3000` 改为 `8192`，保留 `PORT` 环境变量优先和 `Number.isNaN` 回退逻辑。
- Modify: `docs/api/README.md:37` - 把本地启动示例与访问地址从 `3100` 调整到与新默认值一致的 `8192`。

## 实施约束

- 不修改 `docs/superpowers/specs/2026-04-20-server-port-8192-design.md`。
- 不修改 `docs/superpowers/plans/*.md` 下任何历史文档；本次仅新增当前任务计划文件。
- 不新增配置层、帮助函数、环境变量或 CLI 参数。
- 若文档中的端口只是显式自定义示例，只有在它会与“默认端口”认知冲突时才同步到 `8192`；不要扩写无关说明。

### Task 1: 调整 API 运行时默认端口

**Files:**
- Modify: `modules/api/src/server.ts:7-9`

- [ ] Step 1: 先把默认端口常量改为 `8192`，其余解析逻辑保持原样。

```ts
const defaultPort = 8192;
const parsedPort = Number.parseInt(process.env.PORT ?? `${defaultPort}`, 10);
const port = Number.isNaN(parsedPort) ? defaultPort : parsedPort;
```

Run: `git diff -- modules/api/src/server.ts`
Expected: diff 只显示 `defaultPort` 从 `3000` 变为 `8192`，`PORT` 读取与 NaN 回退结构不变。

- [ ] Step 2: 对 API 模块执行最小静态校验，确认改动没有引入格式或类型问题。

Run: `pnpm --filter @aim-ai/api run typecheck && pnpm --filter @aim-ai/api run test:lint`
Expected: 两个命令均通过，不出现与 `modules/api/src/server.ts` 相关的类型或 Biome 报错。

### Task 2: 同步 live 文档中的端口示例

**Files:**
- Modify: `docs/api/README.md:37`

- [ ] Step 1: 将当前 README 中用于本地检查 `/openapi.json` 的启动命令和访问地址改为 `8192`，使文档与默认运行时行为一致。

```md
如果需要本地检查当前 API 侧 JSON 导出，可先运行 `PORT=8192 pnpm --filter ./modules/api run dev`，再访问 `http://localhost:8192/openapi.json`。
```

Run: `git diff -- docs/api/README.md`
Expected: diff 只显示该行中的端口值由 `3100` 改为 `8192`，其余文档内容保持不变。

- [ ] Step 2: 精确搜索当前 live 目标文件，确认本次范围内不再残留旧端口值。

Run: `rg -n "3000|3100|8192" modules/api/src/server.ts docs/api/README.md`
Expected: `modules/api/src/server.ts` 只出现 `8192` 默认端口，`docs/api/README.md` 的本地示例只出现 `8192`，不再命中 `3000` 或 `3100`。

### Task 3: 做最小交付校验并提交实现

**Files:**
- Modify: `modules/api/src/server.ts`
- Modify: `docs/api/README.md`

- [ ] Step 1: 对本次实际交付文件执行一次仓库级最小格式检查，避免把无关文件带入范围。

Run: `pnpm exec biome check modules/api/src/server.ts docs/api/README.md`
Expected: Biome 检查通过；若失败，只修正这两个文件中的格式问题。

- [ ] Step 2: 人工复核最终 diff，确认实现完全覆盖 spec 且没有触碰历史计划文档。

Run: `git diff -- modules/api/src/server.ts docs/api/README.md docs/superpowers/plans/2026-04-20-server-port-8192.md`
Expected: 只看到计划文件和两个 live 文件的最小必要变更；没有历史 `docs/superpowers/plans/*.md` 被改动。

- [ ] Step 3: 提交实现改动，提交信息聚焦默认端口基线调整。

```bash
git add modules/api/src/server.ts docs/api/README.md docs/superpowers/plans/2026-04-20-server-port-8192.md
git commit -m "chore: change server default port to 8192"
```

Run: `git status --short`
Expected: 不再出现已暂存但未提交的本次实现文件；工作区若仍有其他改动，必须与当前任务无关且不被本次提交带入。
