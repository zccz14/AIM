---
"@aim-ai/api": patch
"@aim-ai/contract": patch
"@aim-ai/cli": patch
"@aim-ai/web": patch
---

Treat project ids as UUID values across API persistence, contract validation, CLI fixtures, and web contract fixtures while keeping project paths as path lookup keys.
